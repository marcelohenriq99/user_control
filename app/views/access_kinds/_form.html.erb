<%= form_with(model: access_kind, local: true) do |form| %>
  <% if access_kind.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(access_kind.errors.count, "error") %> prohibited this access_kind from being saved:</h2>

      <ul>
      <% access_kind.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
